# Packed dataset and the official archive -- large, and redistributing the
# sponsor's data is not ours to do.
data/
*.zip

# Keep weights/best.pt (it is a required deliverable) but not intermediates.
models/last.pt
weights/
weights/*.ckpt

# Evaluation scratch
results/bench_out/
results/*.npy

__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.DS_Store

# Trained weights are added at submission time, not committed from a test run.
models/*.pt
models/*.pth
