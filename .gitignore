# Packed dataset and the official archive -- large, and redistributing the
# sponsor's data is not ours to do.
data/
*.zip

# Keep weights/best.pt (it is a required deliverable) but not intermediates.
models/last.pt
weights/
weights/*.ckpt

# Evaluation scratch
results/bench_out/
results/*.npy

__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.DS_Store

# Intermediate checkpoints stay out; models/best.pt is a required submission
# deliverable and is committed explicitly.
models/last.pt
models/*.pth
!models/best.pt
