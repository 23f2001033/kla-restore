# === Required to run the submission entry point (run.py) ==================
# run.py imports nothing beyond these two. No internet access, API keys or
# additional model downloads are needed at inference time.
#
# Install the CUDA build of torch for NVIDIA GPU execution, e.g.:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# Any torch >= 2.1 works; the checkpoint carries no version-specific state.
torch==2.5.1
numpy==2.1.3

# === Additional, only for reproducing training / evaluation ===============
# Not imported by run.py.
PyYAML==6.0.3          # config parsing in train.py
Pillow==12.3.0         # image I/O in the optional PNG/TIFF path
matplotlib==3.11.0     # qualitative figures in evaluate.py
lpips==0.1.4           # perceptual loss for the final fine-tune, and reporting
scikit-image==0.25.2   # metric cross-checks
